<div id="confirm-overlay" class="overlay" aria-modal="true" role="dialog">
  <div class="confirm-box flex-column">
    <div class="confirm-icon">
      <i class="bi bi-exclamation-triangle-fill"></i>
    </div>
    <p id="confirm-message">¿Estás seguro?</p>
    <div class="confirm-actions flex-row">
      <button id="confirm-cancel" class="boton2">Cancelar</button>
      <button id="confirm-ok" class="boton btn-danger">Eliminar</button>
    </div>
  </div>
</div>

<style>
  .confirm-box {
    background: var(--clr-white);
    border: solid 1px var(--clr-border);
    border-radius: 20px;
    padding: 32px 24px 28px;
    width: 90%;
    max-width: 360px;
    gap: 14px;
  }

  .confirm-icon {
    font-size: 2.2rem;
    color: #b02a3a;
    line-height: 1;
  }

  #confirm-message {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--clr-text);
    text-align: center;
    line-height: 1.5;
  }

  .confirm-actions {
    gap: 10px;
    width: 100%;
    margin-top: 6px;
  }

  #confirm-cancel {
    flex: 1;
    background: var(--clr-white2);
    border-color: var(--clr-border);
    color: var(--clr-text);
    font-size: 0.85rem;
    letter-spacing: 0.5px;
  }

  #confirm-ok {
    flex: 1;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
  }
</style>

<script>
  import { createOverlay } from "../scripts/store.ts";

  // _onConfirm vive a nivel de módulo para persistir entre renders
  let _onConfirm: (() => void) | null = null;

  // Re-inicializar en cada navegación para que show/hide siempre
  // operen sobre los elementos del DOM ACTUAL (no los del swap anterior).
  document.addEventListener("astro:page-load", () => {
    const overlayApi = createOverlay("confirm-overlay", {
      onClose: () => { _onConfirm = null; },
    });
    if (!overlayApi) return; // no está en esta página
    const { open, close } = overlayApi;

    const msgEl = document.getElementById("confirm-message")!;
    const cancelBtn = document.getElementById("confirm-cancel")!;
    const okBtn = document.getElementById("confirm-ok")!;

    function show(message: string, onConfirm: () => void) {
      msgEl.textContent = message;
      _onConfirm = onConfirm;
      open();
    }

    // .onclick evita acumulación de listeners si el módulo re-ejecuta
    cancelBtn.onclick = close;
    okBtn.onclick = () => {
      _onConfirm?.();
      close();
    };

    // Actualizar API global con referencias frescas al DOM actual
    (window as any).confirmDialog = { show, hide: close };
  });
</script>
