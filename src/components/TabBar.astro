<div class="tab-bar" role="tablist">
  <button class="tab-btn" data-tab="visitados" role="tab" aria-selected="false">
    <i class="bi bi-check2-circle tab-icon"></i>
    <span class="tab-label">Visitados</span>
    <span class="tab-count" id="count-visitados">0</span>
  </button>
  <button
    class="tab-btn"
    data-tab="pendientes"
    role="tab"
    aria-selected="false"
  >
    <i class="bi bi-bookmark tab-icon"></i>
    <span class="tab-label">Pendientes</span>
    <span class="tab-count" id="count-pendientes">0</span>
  </button>
</div>

<style>
  .tab-bar {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    width: 100%;
  }

  .tab-btn {
    gap: 8px;
    padding: 12px 16px;
    border-radius: 14px;
    border: 1.5px solid var(--clr-border);
    background: var(--clr-white);
    color: var(--clr-text);
    letter-spacing: 0;
    text-transform: none;
    transition: 0.18s ease;
  }

  .tab-btn.active {
    background: var(--clr-primary);
    border-color: var(--clr-primary);
    color: var(--clr-white);
  }

  .tab-icon {
    font-size: 1rem;
  }

  .tab-label {
    font-size: 0.82rem;
    font-weight: 600;
    letter-spacing: 0.3px;
  }

  .tab-count {
    font-size: 0.72rem;
    font-weight: 700;
    min-width: 20px;
    height: 20px;
    line-height: 20px;
    padding: 0 6px 0 5px;
    border-radius: 20px;
    display: inline-block;
    text-align: center;
    background: rgba(0, 0, 0, 0.1);
    letter-spacing: 1px;
    box-sizing: border-box;
  }

  .tab-btn.active .tab-count {
    background: rgba(255, 255, 255, 0.25);
  }

  @media (hover: hover) {
    .tab-btn:not(.active):hover {
      border-color: var(--clr-border2);
      color: var(--clr-primary);
    }
  }

  @media (min-width: 768px) {
    .tab-bar {
      gap: 12px;
    }

    .tab-btn {
      padding: 13px 20px;
    }

    .tab-label {
      font-size: 0.88rem;
    }
  }
</style>
